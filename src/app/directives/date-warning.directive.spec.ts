import { DateWarningDirective } from './date-warning.directive';

describe('DateWarningDirective', () => {
  it('should create an instance', () => {
    const directive = new DateWarningDirective();
    expect(directive).toBeTruthy();
  });
});
